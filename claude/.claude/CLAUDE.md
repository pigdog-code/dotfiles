<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Jan 22, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #587 | 6:30 PM | 🔵 | Permission configuration for MCP servers and tools discovered | ~361 |
</claude-mem-context>

# Language Settings

- 지원 언어: 영어(English), 한국어(Korean)
- 이 설정은 유저의 영어 연습을 위한 요청사항입니다.

## 영어 요청 시 처리 규칙

유저가 **영어로** 문의/요청할 경우, 다음 순서로 응답하세요:

1. **한국어 해석**: 유저의 영어 문장을 한국어로 번역하여 보여줍니다.
2. **Best Practice (영어)**: 유저의 영어 문장에 대해 더 자연스럽거나 적절한 영어 표현(Best Practice)을 제안합니다.
3. **작업 수행**: 요청사항에 대한 실제 작업을 진행합니다.

## 한국어 요청 시 처리 규칙

유저가 **한국어로** 문의/요청할 경우, 다음 순서로 응답하세요:

1. **Best Practice (영어)**: 유저의 한국어 문장을 자연스러운 영어 표현으로 제안합니다.
2. **작업 수행**: 요청사항에 대한 실제 작업을 진행합니다.

## 답변 언어 규칙

- 답변은 **한국어와 영어 모두** 제공합니다.
- 단, 토큰 제한이 있거나 내용이 너무 길 경우 **한국어를 우선**으로 보여줍니다.

# 문서 작성 시 도메인 지식 포함

- 유저는 **주니어 개발자**입니다
- 문서 작성 시 관련 **도메인 지식(배경 개념)**도 함께 설명합니다
  - 예: AWS IAM 구조, 네트워크 개념, 빌드 시스템 동작 원리, DB 설계 패턴 등
- "왜 이렇게 동작하는지"에 대한 설명을 포함하여, 나중에 문서만 봐도 이해할 수 있도록 작성합니다
- 단, 코드 주석에는 불필요한 설명을 넣지 않습니다 (문서와 코드는 분리)

# 마크다운 작성 규칙

- 마크다운 테이블 작성 시 각 컬럼의 separator(`---`)와 셀 내용을 패딩하여 파이프(`|`)가 수직으로 정렬되도록 합니다

# CLAUDE.md 계층 규칙

- 유저가 "전체 프로젝트에 반영해줘", "모든 프로젝트에 적용해줘", "글로벌 규칙으로 추가해줘" 등 **모든 프로젝트에 적용되는 규칙**을 요청하면, 반드시 이 파일(`~/.claude/CLAUDE.md`)에 작성합니다
- 하위 프로젝트의 `CLAUDE.md` (예: `~/work/code/CLAUDE.md`, 프로젝트별 `CLAUDE.md`)에 작성하지 않습니다
- 특정 프로젝트에만 해당하는 규칙은 해당 프로젝트의 `CLAUDE.md`에 작성합니다

# 작업 문서 운영 방식 (필수 — 반드시 준수)

> **⚠️ 핵심 원칙**: "계획을 세워라" = "반드시 문서 파일(`.md`)을 생성하라"
> 대화 컨텍스트(채팅창)에만 계획을 표시하는 것은 **절대 금지**입니다.

모든 비자명(non-trivial) 코딩/설계 작업은 **문서 기반**으로 계획을 수립한 후 진행합니다.
Agile의 Epic/Story 개념을 차용하여, 상위 계획(Epic)과 하위 작업(Story)으로 구조화합니다.

```
Epic (상위 계획)
├── Story (하위 작업 1)
├── Story (하위 작업 2)
└── Story (하위 작업 3)
```

## 문서 유형

**Epic (상위 계획 문서)**
- Milestone 또는 Phase를 정리한 상위 추적 문서
- 포함 내용: 배경/목적, Milestone/Phase 테이블 (상태 컬럼), 하위 Story 링크, 의존성
- 위치: 프로젝트 구조에 맞게 자유 배치
- 생성 시점: 작업 진행 전, 유저 confirm 후

**Story (하위 작업 문서)**
- Epic 하위의 개별 구현 단위
- 포함 내용: 상위 Epic 역참조 링크, 배경/목적, 상세 작업 내용, 완료 조건
- 위치: `tasks/` 폴더
- 생성 시점: Epic 작성 시 함께 생성하거나, 필요 시 추가

**적용 대상 외** — 단순 분석/조사, backlog, 인프라 가이드, 회의록 등은 접두어 없이 자유 네이밍

## 파일 네이밍 규칙

**Epic:**
```
epic-{epic-name}.{상태}.md
```

**Story:**
```
story-{epic-name}-{p1|m1}-{story-name}.{상태}.md
       ~~~~~~~~~~  ~~~~~~   ~~~~~~~~~~~
       선택적       선택적    필수
```
- `{epic-name}` — 상위 Epic이 있을 경우
- `{p1|m1}` — Phase(`p`) 또는 Milestone(`m`) 번호가 있을 경우
- `{story-name}` — 작업 주제 (하이픈 허용)

예:

| 케이스                   | 파일명                                                    |
|--------------------------|-----------------------------------------------------------|
| Epic + Phase + 주제      | `story-lsw-2026-proposal-p2-unified-admin.in-progress.md` |
| Phase + 주제 (Epic 생략) | `story-p3-admin-productivity.todo.md`                     |
| 주제만 (독립 Story)      | `story-fix-login-bug.todo.md`                             |

**상태 suffix:** `.todo` → `.in-progress` → `.done`
상태 변경 시 파일명을 리네임하고, 해당 파일을 참조하는 다른 문서의 링크도 함께 업데이트합니다.

## 작업 흐름

1. **계획 수립**: 유저에게 Plan 문서 작성 여부를 물음 → 동의 시 폴더/파일명 확인 후 Epic 생성 → 하위 Story 생성 → 유저 confirm 후 작업 시작
2. **작업 진행**: Story `.todo` → `.in-progress` → 구현 → `.done` / Epic의 Milestone 테이블 상태 업데이트 (🔲 → ✅)
3. **Phase 완료 시**: Epic에 구현 결과 업데이트 (완료일, 실제 구현 내용, 원안과 달라진 점)
4. **전체 완료 시**: Epic `.in-progress` → `.done`

## 기존 파일의 Epic/Story 분류 및 리네이밍

기존 작업 파일(마일스톤/Phase 테이블 포함)을 Epic/Story 규칙에 따라 재분류:

### 분류 기준

**Epic으로 분류:**
- Milestone 또는 Phase 테이블을 최상위 단계에 가진 문서
- 독립적인 프로젝트/이니셔티브
- 상위 문서가 없는 자체 완결 작업

**Story로 분류:**
- "상위 문서" 필드가 명시된 파일
- 다른 Epic 문서의 Milestone/Phase에 포함되는 하위 작업
- 특정 Epic의 구성 요소

### 리네이밍 전략

1. **각 파일의 상위 문서 확인**
   - 파일 상단의 "상위 문서", "상위 Plan" 등 역참조 링크 확인
   - 명시되지 않은 경우 파일 내용 분석 (마일스톤 구조, 작업 단계)

2. **상위 Epic 매핑 및 Epic 파일 생성**
   - 상위 Epic이 명시된 경우 → 그 Epic을 `epic-{name}.done.md` 형태로 생성/리네임
   - 상위 Epic이 없는 경우 → 파일 자체를 Epic으로 분류

3. **하위 Story 리네이밍**
   - 상위 Epic이 있으면: `story-{epic-name}-{story-name}.{상태}.md`
   - Epic이 여러 Story를 포함하면: `story-{epic-name}-m{n}-{story-name}.{상태}.md` (Milestone 번호 포함)

### 예시

**Before (기존):**
```
tasks/
├── lsw-2026-privacy-access-control.done.md      (상위: Gap 분석)
├── lsw-2026-privacy-session-timeout.done.md     (상위: Gap 분석)
└── lsw-2026-infra-migration-plan.done.md        (독립)
```

**After (변경):**
```
tasks/
├── epic-lsw-privacy-checklist.done.md           (Epic 새 생성 또는 리네임)
│   ├── story-lsw-privacy-checklist-m1-access-control.done.md
│   └── story-lsw-privacy-checklist-m2-session-timeout.done.md
└── epic-lsw-infra-migration.done.md             (독립 Epic)
```

## 금지 사항

- ❌ 대화 컨텍스트에만 계획을 텍스트로 보여주고 파일을 생성하지 않는 행위
- ❌ Plan 문서 없이 코드를 읽고 분석만 시작하는 행위 (분석도 Plan 문서 먼저)
- ❌ 유저 confirm 없이 작업을 진행하는 행위

# Git 커밋 메시지 규칙 (필수 — 반드시 준수)

> **⚠️ 핵심 원칙**: 커밋 메시지는 **그 자체만으로 변경 내용을 완전히 이해할 수 있어야** 합니다.
> 외부 문서(Plan, Task 등)를 참조해야만 의미를 파악할 수 있는 식별자는 **절대 포함하지 않습니다**.

- 커밋 메시지는 **상세하게** 작성하되, **커밋 메시지만으로 확인할 수 없는 외부 참조는 제외**합니다
- **포함해야 할 내용**: 변경 사항의 목적, 구체적인 변경 내용, 발견/수정한 버그 등
- **❌ 제외해야 할 내용** (위반 시 amend 필요):
  - Plan 문서 번호, 내부 Task ID (예: `t5.3`, `Task 2.1`)
  - 마일스톤 ID (예: `M4`, `M7`)
  - 이슈 번호 (예: `ISSUE-1`, `ISSUE-3`) — Plan/Task 문서에서만 의미를 갖는 경우
  - TC 번호 (예: `A-09`, `C-06`)
  - 대화 컨텍스트에서만 확인 가능한 참조
  - Plan 문서는 Git 저장소에 포함되지 않을 수 있으므로, 커밋 메시지만 보고도 변경 내용을 이해할 수 있어야 합니다

# 문서 메타데이터 및 타임로그

## 작업 문서 (Epic/Story) 타임로그

Epic/Story의 마일스톤 테이블에 타임로그 컬럼 추가:

```markdown
| #    | 작업                      | 상태    | 완료일     | 소요시간 | 설명                          |
|------|--------------------------|---------|-----------|---------|-------------------------------|
| 1.1  | PAT 스키마 확장           | 🔲 대기 | -         | -       | mcp_tokens에 컬럼 추가        |
| 1.2  | McpAuth IP 화이트리스트   | 🔲 대기 | -         | -       | allowed_ips 기반 로직         |
```

**기록 시점:**
- **완료 시**: `완료일` (YYYY-MM-DD) + `소요시간` (1.5h, 2h 등) 입력
- **Phase 완료 후**: SESSION-CONTEXT.md에 "P1: 총소요시간, 주요 학습" 한 줄 요약

---

## 분석/참조 문서 메타데이터

분석, 참고, 설계 등 작업 문서가 아닌 모든 문서의 상단에 메타데이터 추가:

```markdown
> 작성: 2026-03-31 (2.5h) | 업데이트: 2026-04-01 (0.5h)
```

**형식**: `작성: YYYY-MM-DD (시간) | 업데이트: YYYY-MM-DD (시간)`

**기록 시점:**
- **문서 작성 완료 시**: 작성일 + 소요시간
- **문서 수정/보강 시**: 업데이트 항목 추가

**목적**: 
- 문서의 신선도 파악
- 유사 문서 작성 시 시간 추정 데이터
- 언제 마지막으로 검토되었는지 확인

---

# TIL (Today I Learned) - 개인 지식 저장소

- **경로**: `~/.claude/til/`
- **호출 방법**: 유저가 "TIL에 저장해줘" 라고 요청하면 대화 내용을 정리하여 해당 폴더에 md 파일로 저장합니다.
- **대상**: 프로젝트와 무관한 일반 기술 학습 내용 (언어, 도구, 개념 등)
- **파일 네이밍**: `{주제}.md` (예: `php-composer.md`, `git-rebase.md`)
- **작성 규칙**:
  - 핵심 개념을 간결하게 정리
  - 다른 언어/도구와의 비교가 있으면 포함
  - 실무에서 바로 참고할 수 있는 형태로 작성